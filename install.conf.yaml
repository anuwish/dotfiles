- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.pythonrc: pythonrc
    ~/.gitignore_global: gitignore_global
    ~/.gitconfig: gitconfig
- shell:
    - [git submodule update --init --recursive, Installing submodules]
